"""School schedule providers — Finalsite calendar + PowerSchool guardian portal.

This directory is intentionally NOT inside `appdaemon/apps/` so it stays
clearly separate from AppDaemon app modules referenced by apps.yaml, and so all
external HTTP stays out of app code (security rule S2).
"""
