"""
AI provider interfaces + implementations used by AppDaemon apps.

This directory is intentionally NOT inside `appdaemon/apps/` so it stays clearly
separate from AppDaemon "apps" (modules/classes referenced by `apps.yaml`).
"""

