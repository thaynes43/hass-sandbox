# AppDaemon - https://pypi.org/project/appdaemon/
appdaemon>=4.5.0
# Async HTTP (Immich fetcher)
aiohttp>=3.9
# Testing
pytest>=7.0
pytest-asyncio>=0.23
coverage>=7.0
# Deploy tooling (keeps YAML tags like !secret intact when merging)
ruamel.yaml>=0.18
