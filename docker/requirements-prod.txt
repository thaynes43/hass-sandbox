# Runtime dependencies not included in the base acockburn/appdaemon image.
# Dev-only packages (pytest, ruamel.yaml, coverage) are excluded.
python-dotenv>=1.0
# IANA timezone data for zoneinfo (protect_health_checker active-hours
# math) — guarantees ZoneInfo works even if the base image lacks tzdata.
tzdata>=2024.1
